#include <stdio.h>

int main()
{
  int firstNumber;
  int secondNumber = 0;

  printf("What is the first number ? ");
  scanf("%d", &firstNumber);
  printf("%d \n", firstNumber);
  printf("What is the second number ? ");
  scanf("%d", &secondNumber);
  printf("%d \n", secondNumber);


  
  return 0;
}
